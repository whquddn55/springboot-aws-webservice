spring:
  profiles:
    include: oauth
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springaws
    username: root
    password: whxnxl55;;
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  servlet:
    encoding:
      force-response: true